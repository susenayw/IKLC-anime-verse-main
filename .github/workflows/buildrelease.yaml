name: Build and Release APK

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Flutter APK
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 1. Setup Java
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 2. Setup Flutter (Sets up the Android SDK and makes $ANDROID_HOME available)
      - name: Set up Flutter and Android SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.3'
          channel: 'stable'
          cache: true

      # 3. FIX: Accept Licenses and Install NDK (The definitive CI/CD fix)
      # This resolves the recurring 'LicenceNotAcceptedException'.
      - name: Accept Android SDK licenses and Install NDK
        run: |
          SDK_MANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          
          # Install the specific NDK version
          $SDK_MANAGER --install "ndk;29.0.13113456"
          
          # Create the necessary 'licenses' directory
          mkdir -p $ANDROID_HOME/licenses
          
          # Manually create the required license files that Gradle checks for
          echo "8913d648756c9a49b6863d0a9b897825591cd69e" > $ANDROID_HOME/licenses/android-ndk-license
          echo "24333f8a63b6825ea9c5514f83c2829b004d1fee" > $ANDROID_HOME/licenses/android-sdk-license
          echo "84831b94d1e9dbbe9201946bb8979e8a52026e3a" > $ANDROID_HOME/licenses/android-sdk-preview-license
          
          echo "All required licenses have been manually accepted."


      - name: Install Dependencies
        run: flutter pub get

      # 4. Decode Keystore
      # File is placed at: ./android/app/upload-keystore.jks
      - name: Decode and Save Keystore File
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 --decode > android/app/upload-keystore.jks

      # 5. FIX: Create key.properties (Correct path for Gradle)
      # File is placed at: ./key.properties (Repository Root)
      - name: Create key.properties File
        env:
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: |
          # Writing to the root of the project (key.properties)
          cat > key.properties << EOF
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          # Defining the path to the keystore from the project root
          storeFile=android/app/upload-keystore.jks
          EOF
          echo "key.properties created at repo root."

      - name: Decode google-services.json
        env:
          GOOGLE_SERVICES_JSON_BASE64: ${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}
        run: |
          echo "$GOOGLE_SERVICES_JSON_BASE64" | base64 --decode > android/app/google-services.json

      - name: Build Release APK
        run: flutter build apk --release

      - name: Get Version
        id: get_version
        run: |
          VERSION=$(grep '^version: ' pubspec.yaml | cut -d ' ' -f 2 | cut -d '+' -f 1)
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: anime-verse-${{ steps.get_version.outputs.version }}
          path: build/app/outputs/flutter-apk/app-release.apk

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: anime-verse-${{ needs.build.outputs.version }}
          path: ./

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: app-release.apk
          body: |
            ## Anime Verse v${{ needs.build.outputs.version }}
            
            ### 📱 Download
            Download the APK below and install on your Android device.
            
            ### ✨ What's New
            - Automated build via GitHub Actions
            - Release signed with production keystore
            
            ### 🔒 Security
            - Signed with release keystore
            - Firebase configured for production
            - Firestore security rules enabled
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}